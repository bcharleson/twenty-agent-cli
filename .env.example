# Required — your Twenty instance base URL (no trailing slash)
TWENTY_BASE_URL=https://crm.example.com

# Required — API key from Settings → API & Webhooks
TWENTY_API_KEY=your_api_key_here

# Optional — workspace subdomain for multi-tenant routing (rare on self-hosted)
# TWENTY_WORKSPACE_SUBDOMAIN=
